name: Disabled legacy sync volley to slime-volley

on:
  workflow_dispatch:

permissions:
  contents: none

concurrency:
  group: sync-volley
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Sync disabled
        run: |
          echo "The legacy sync from slime-wk2026/volley to StefanAIpel/slime-volley is disabled."
          echo "Edit and deploy Slime Volleyball from the dedicated StefanAIpel/slime-volley repository."
